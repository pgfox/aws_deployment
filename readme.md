
To setup the conneciton information - export the following env vars:

``` export AWS_ACCESS_KEY_ID="AKI..."
    export AWS_SECRET_ACCESS_KEY="..."
    export AWS_DEFAULT_REGION="eu..."

These are the IAM users access keys that has the appropriate permissions.
